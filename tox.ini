[tox]
envlist = py27
skipsdist = True

[tox:travis]
2.7 = py27

[testenv]
usedevelop = True
install_command = pip install -U {opts} {packages}
deps = .[test]
whitelist_externals = make
setenv = TWISTD_SCRIPT={envbindir}/twistd
commands =
    make check
